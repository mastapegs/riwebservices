import { createMuiTheme } from "@material-ui/core";
// import { blue } from "@material-ui/core/colors";

const theme = createMuiTheme({
    // Example of primary color override
    // palette: {
    //   primary: blue
    // },
});

export default theme;